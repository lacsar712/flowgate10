# Flowgate · OPC-UA 订阅死区闸门

## 这不是什么

不是工作流引擎、不是「审批流」、不是预约系统。这是工业现场 **OPC-UA monitored item** 的死区（deadband）闸：值变化小于死区就不往信息层送。

## 谁在用

离散制造车间的采集工控机。PLC 经 OPC-UA 上报模拟量（温度、压力、轴转速）。若不做死区，总线会被噪声打满。本产品只做订阅项登记与死区计算，不做 MES/排产。

## 核心业务

1. 正文为 `eu=`（工程值）和 `deadband=`（绝对值死区）。
2. 死区必须 ≥ 0，且不得超过 `|eu|` 的 10%（eu=0 时死区必须为 0）。
3. 标签是节点浏览名，如 `Kiln.Temp`。
4. 订阅项变更写入修订；附件是 UA 节点导出 XML。
5. 发布前流水线：validate deadband → index node → export。

## 运行与验收

- `eu=100 deadband=20`（超过 10%）必须拒绝。
- `eu=100 deadband=1` 必须接受。
- 控制台用于登记订阅项，不是看板图表产品。
